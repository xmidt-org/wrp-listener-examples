# SPDX-FileCopyrightText: 2022 Comcast Cable Communications Management, LLC
# SPDX-License-Identifier: Apache-2.0
---
name: CI

on:
  push:
    branches:
      - main
    paths-ignore:
      - README.md
      - CONTRIBUTING.md
      - MAINTAINERS.md
      - LICENSE
      - NOTICE
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  pull_request:
  workflow_dispatch:

jobs:
  basicListener:
    uses: xmidt-org/shared-go/.github/workflows/ci.yml@fe1aac86f0c6b5070e7bda262d3c31cb53cc510c # v4.12.8
    with:
      lint-skip:            true
      release-skip-publish: --skip-publish
      release-type:         program
      release-main-package: basicListener
      working-directory:    basicListener
    secrets: inherit
  configurableListener:
    uses: xmidt-org/shared-go/.github/workflows/ci.yml@fe1aac86f0c6b5070e7bda262d3c31cb53cc510c # v4.12.8
    with:
      lint-skip:            true
      release-skip-publish: --skip-publish
      release-type:         program
      release-main-package: configurableListener
      working-directory:    configurableListener
    secrets: inherit
  timedListener:
    uses: xmidt-org/shared-go/.github/workflows/ci.yml@fe1aac86f0c6b5070e7bda262d3c31cb53cc510c # v4.12.8
    with:
      lint-skip:            true
      release-skip-publish: --skip-publish
      release-type:         program
      release-main-package: timedListener
      working-directory:    timedListener
    secrets: inherit
